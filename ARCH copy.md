# ARCH — 多周期因子交易系统架构

目标形态: 7 层因子库 (1m / 15m / 1h / 隔夜 / 1d / 5d / 15d) → 4 个独立账本 → 1 个中央执行器, A 股现货纯多头实盘.
本文是架构指导文件: 每层给出精确定义 (变量/公式/算法), 并与业界主流设计对比 (主流做法 / 本设计 / pro / con / 隐含假设).

---

## 0. 硬约束与第一性原则

**硬约束** (交易所/账户属性, 不可协商):

| 约束     | 内容                                        | 对架构的直接后果                                   |
| -------- | ------------------------------------------- | -------------------------------------------------- |
| T+1      | 当日买入不可当日卖出                        | 半衰期 < 1d 的信号多头侧不可独立变现, 只能改执行价 |
| 涨跌停   | 涨停买不进, 跌停卖不出 (物理); 停牌不可交易 | 执行层必须建模"意图 ≠ 成交", 偏差跨日滚动          |
| 满仓     | 目标仓位 ≈ 100%, 无择时                     | alpha 是纯截面问题; 快信号只能表达为"换谁/何时换"  |
| 离散手数 | 100 股整数倍                                | 真实账户与目标权重之间永远存在取整跟踪误差         |
| 纯多头   | 不可做空                                    | 预测只在截面相对意义上可变现; 负 alpha = 不持有    |

**第一性原则** (后文以 P1–P5 引用, 每个设计选择都能追溯到其中之一):

- **P1 参数预算跟样本量走**: 每层模型的复杂度由该层*独立*样本数决定. 月度级信号 (十年 ≈ 百个独立观测) 只配线性+收缩; 分钟级信号 (亿级样本) 才配非线性 ML. 复杂度用错层是本领域过拟合的第一来源.
- **P2 现状免费, 改变收费**: 持有不动零成本, 任何交易付即时往返成本. 举证责任永远在"动"的一侧: 期望净增益必须同时越过成本门槛与不确定性门槛.
- **P3 拟合与验收分离**: 让数据*选参数* (拟合) 的地方尽量少; 让数据只*行使否决权* (验收) 的地方尽量多. 独立样本不足的结构 (跳跃收益、事件规则) 一律只验收不拟合: 参数由业务先验定死, 事件研究检查数据是否否决.
- **P4 聚合在仓位空间, 不在信号空间**: 不建统一的多周期信号模型 (期限核/多任务), 而是每个交易周期一个独立账本, 各自简单; 真实仓位 = 账本仓位加权和. 模块化、可独立验收、加账本不动存量.
- **P5 单一货币**: 一切预测折算为 bps (预期超额收益), 一切决策是 bps 与 bps (含成本) 的比较. rank/z 分跨周期不可加, bps 可加.

**分层总览** (数据流自上而下; 验收层旁路监督一切; 规则层对执行层 override):

```text
L1 数据/PIT ─ L2 特征 ─ L3 因子库(×7) ─ L4 预测(×4账本) ─ L5 组合(×4账本) ─ L6 聚合(×1) ─ L7 执行(×1) → 市场
                                                                                              ↑
                                                                            L8 规则层 (事件 override, 最高优先级)
                    L9 验收/归因层 (旁路: 一切因子/账本/规则的准入、存续、资金配比)
```

---

## 1. 数据与 PIT 层

**核心不变量**: 任意时点 t 的任何下游量, 只依赖 t 时点已可见的数据. PIT 消化一次性发生在数据入库→张量阶段, 下游 (因子/模型/回测/实盘) 永不再做时间偏移 — 单点消化杜绝逐处理漏偏移.

**定义**:
- `visible_date(rec)`: 记录 rec 对市场参与者可见的日期 (公告日/入库日, 取保守者).
- 日频 cutoff: 交易日 T 的行, 可见集 = `{rec : visible_date(rec) ≤ T + offset}`, offset ∈ {−1, 0}. offset = −1 为默认 (承认盘后入库滞后); offset = 0 仅限"T 日开盘前业务上已确定"的字段 (如 ST/停牌标识), 且实盘当日行须由真盘前快照填充.
- 分钟频 cutoff: bar 粒度. bar t 的特征只用 `[.., t]` 已关闭 bar 的数据; 决策在 bar t 关闭时刻, 执行最早发生于 bar t+1 (一 bar 执行滞后, 消除同 bar 前视).
- 价格: 收益计算用后复权连续价; 委托/成交用原始价. 分红送转日两者的换算因子当日盘前可见.

**跨次修正**: 数据修订以新 visible_date 发布, 历史行不改写 ⇒ 任意历史时点 replay 自动取"当时可见版本". 回测与实盘用同一 cutoff 代码.

| 对比       | 主流做法                                  | 本设计                           | pro                      | con                      | 假设                      |
| ---------- | ----------------------------------------- | -------------------------------- | ------------------------ | ------------------------ | ------------------------- |
| PIT 实现   | 买 vendor PIT 库 / 研究端逐因子手工 shift | 入库层单点 cutoff, 下游零偏移    | 泄漏面收敛到一处, 可审计 | 新数据源接入成本前置     | 服务端 PIT 语义可信       |
| 公告时间戳 | tick 级公告时间对齐                       | 日级保守 (盘前公告并入 next-day) | 简单, 回测=实盘          | 放弃盘前公告的当日 alpha | 该 alpha 量级小于实现成本 |

---

## 2. 特征层

**定义**: 特征 = 对 PIT 数据的确定性变换, 组织为编译期/静态可校验的 DAG. 节点分时序 (per 标的沿时间, 可并行于标的) 与截面 (per 时点沿标的, 可并行于时间) 两类, 时序节点禁止依赖截面节点 (保证增量计算与实盘逐 bar 更新可行).

**不变量**:
- 无前瞻: 节点在 t 的值只依赖祖先节点 `[.., t]` 的值 (静态校验依赖方向, 运行期抽查).
- NaN 语义: 不可得就是 NaN (上市前/停牌/退市后), 不人为补 0; 可用性由下游显式判断.
- 单点真理: 每个特征的公式/假设与实现同源同文件, 文档不维护副本.

| 对比     | 主流做法                    | 本设计               | pro                          | con          | 假设           |
| -------- | --------------------------- | -------------------- | ---------------------------- | ------------ | -------------- |
| 特征管理 | 运行时 feature store + 配置 | 静态 DAG, 代码即注册 | 零运行时漂移, 依赖环编译期死 | 迭代需重编译 | 研究员即工程师 |

---

## 3. 因子库层 (7 库)

**库的定义**: 库 = 按*原生 horizon* 归组的因子集合. 库是研究组织单位, **不是**交易单位 (交易单位是账本, 见 L4). 一个因子可以被多个账本消费.

| 库   | 原生 horizon    | 主数据源            | 独立样本量级 (十年)    | 库内允许的组合复杂度 (P1) |
| ---- | --------------- | ------------------- | ---------------------- | ------------------------- |
| 1m   | 分钟~十分钟     | tick/1m bar         | 10⁸⁺                   | GBDT/NN                   |
| 15m  | 十分钟~小时     | 1m/15m bar          | 10⁷                    | GBDT                      |
| 1h   | 小时~当日尾盘   | 15m/1h bar          | 10⁶                    | GBDT                      |
| 隔夜 | close→next open | 日频+尾盘微结构     | 10⁵~10⁶ (截面×2500 日) | GBDT (浅) / 线性          |
| 1d   | 1~3 日          | 日频行情/资金流     | ~2500 日 (截面平均后)  | 线性 (+收缩)              |
| 5d   | 一周            | 日频+低频事件       | ~500 独立窗            | 线性 (+收缩)              |
| 15d  | 二至六周        | 财务/估值/股东/事件 | ~150 独立窗            | 线性 (+强收缩)            |

**因子定义**: 因子 k 在时点 t 的值是截面标准分 `z_k,i(t)`, i ∈ 当日可交易池. 管线:

```text
raw → mask(未上市/退市/不可得=NaN) → winsorize(MAD, k=3, 仅对因子值) → 截面 z → (可选: 行业+log市值中性化)
```

因子**无方向**: 好坏由 IC 符号携带, 方向属于预测层. 因子值*可* winsorize; **label 永不 winsorize** (见 L4, P3: 右尾跳跃是本策略域 alpha 的主要载体, 截掉右尾 = 把要赚的钱从测量里删掉).

**准入协议 (档案卡)** — 每个候选因子入库前产出一张档案卡, 全库统一口径:

- **IC 曲线**: `IC_k(h) = mean_t SpearmanCorr_i( z_k,i(t), y_i(t,h) )`, h ∈ {1, 5, 20, 60} 日 (日内库用 {1bar, 5bar, 当日剩余, 隔夜}). Spearman 而非 Pearson: label 含跳跃, 秩统计天然稳健且不需截尾.
- **误差棒**: 时间块 bootstrap, 块长 ≥ 2h (吃掉 label 重叠自相关), 得 `se_k(h)`.
- **收缩** (经验贝叶斯, 收缩向 0): `IC*_k(h) = IC_k(h) · τ²(h) / (τ²(h) + se_k²(h))`, τ²(h) = 全库因子 IC(h) 的横截方差. 样本少的因子自动被压向 0 — 30d 级因子十年只有 ~150 个独立观测, 不收缩必过拟合 (P1).
- **准入线**: 在目标账本 horizon W_b 上 `|IC*_k(W_b)| / se_k(W_b) ≥ 2`.
- **持续性**: top 集保留率 `ρ_k(h) = mean_t |Top_q(t) ∩ Top_q(t+h)| / |Top_q(t)|` (q = 目标账本持仓宽度) → 该因子驱动交易时的换手先验.
- **半衰期**: 对 `IC_k(h) ≈ IC_k(1)·exp(−h/H_k)` 拟合 H_k. **H_k 只作账本路由的先验, 不进任何模型** — 半衰期是描述量, 不是参数.
- **事件曲线** (跳跃型因子): 入选对齐的累计超额 `CAR_k(τ) = mean[入选后第 τ 日累计超额]`, 看命中率×赔率结构, 供人审, 不进模型.

| 对比     | 主流做法                  | 本设计                                 | pro                              | con                        | 假设               |
| -------- | ------------------------- | -------------------------------------- | -------------------------------- | -------------------------- | ------------------ |
| 因子评价 | alphalens 式 1d IC + 分层 | 档案卡: IC(h) 全曲线 + ρ(h) + 事件曲线 | 半衰期/换手/跳跃结构入档, 可路由 | 每因子评价成本高           | 因子数百级而非万级 |
| label    | winsorize 收益后算 IC     | label 不截尾 + Spearman                | 保留跳跃 alpha                   | IC 噪声更大 (靠窗口平均补) | 跳跃与因子截面相关 |
| 方向     | 因子带预设方向            | 无方向, IC 符号定向                    | 挖掘空间对称, 防人为先验错误     | 档案卡必须先行             | —                  |

---

## 4. 预测层 (4 账本)

**账本的定义**: 账本 b = 一个独立的"预测 + 组合"进程, 拥有唯一的交易 horizon W_b、独立资金配比 c_b、独立台账. **库 ≠ 账本**: 7 库路由进 4 账本. 多周期聚合的答案在这里 (P4): *不聚合* — 每账本 label 定在自己实际交易的 horizon 上, 让各自的模型消化各速度特征; 跨周期在 L6 仓位空间相加.

| 账本 | W_b               | label (精确定义)                                   | 特征来源 (路由先验*) | 模型          |
| ---- | ----------------- | -------------------------------------------------- | -------------------- | ------------- |
| 慢   | ≈ 10–15 交易日    | y = P(t_exec+W)/P(t_exec) − 1, t_exec = 决策日收盘 | 15d, 5d (+1d 辅助)   | 线性 + 收缩   |
| 中   | ≈ 1–3 交易日      | 同上, W = 2d                                       | 1d, 5d, 隔夜         | 线性或浅 GBDT |
| 隔夜 | close → open      | y = Open(t+1)/Close(t) − 1                         | 隔夜, 1h, 15m        | GBDT          |
| 日内 | 30m–2h (仅老库存) | y = P(bar t+1+H)/P(bar t+1) − 1                    | 1m, 15m, 1h          | GBDT          |

*路由先验: 账本 b 默认吸收 `H_k ∈ [W_b/5, 20·W_b]` 的因子, 档案卡证据可 override. 半衰期先验砍搜索空间是正则化, 不是硬约束.

**慢/中账本 (线性, P1)** — 多因子 Grinold-GLS:

```text
β = (R_z + γI)⁻¹ · IC*(W_b)          # R_z: 因子 z 分相关阵, Ledoit-Wolf 收缩; γ: 岭系数
μ_i(t) = σ_CS(W_b) · Σ_k β_k · z_k,i(t)   # σ_CS: W_b-horizon 收益的截面离散度 (滚动估计)
```

- 共线性由 `(R_z+γI)⁻¹` 处理 (等价多元 GLS 回归; 朴素 IC 加权 = 错误假设因子正交).
- 备选等价物: 带稳健性装置的格点权重搜索 (单纯形格点 + 滑窗分层稳定性评分 + 邻域敏感性惩罚), 评价 label 同为 y(t, W_b) 且**含成本罚项** (顶部组合逐日换手 τ, 总分用 ᾱ − cost·τ). 两法应给出方向一致的权重, 互为 sanity check; 长期二选一.
- 30d 级因子的自相关: 预测端无需处理 (相邻预测相似 = 低换手, 是特性不是缺陷); 只影响 IC 推断 ⇒ 已由块 bootstrap + 收缩覆盖.

**隔夜/日内账本 (GBDT, P1)**:

- 全库因子做特征 (路由先验筛), 一套全局超参 (深度/叶子/正则固定, 不 per-factor 调).
- **purged walk-forward** (泄漏控制的全部机制, 与模型无关):
  ```text
  train [t0, t1−H) → purge [t1−H, t1) → test [t1, t2) → embargo [t2, t2+e)
  H = label 前瞻长度, e ≈ 1 日; 按月滚动; 截面整块分折 (同时段全部标的同折, 禁随机打散)
  ```
- **重叠样本**: 分钟网格逐点采样时相邻 label 几乎全重叠, 有效样本量比名义少 1–2 个数量级. 处理: 采样步长 stride = H, 或按 uniqueness (1/重叠计数) 加权. 不处理则 CV 分数系统性虚高 — 这是分钟库最大的隐性过拟合源, 优先级高于 purge 本身.

**bps 校准 (P5)**: 每账本滚动回归 `y = a + b·μ`, 要求 b ∈ [0.8, 1.2]; 超界即用 b 重标定 μ. 校准后各账本的 μ 同为"该 horizon 上的预期超额 bps", 跨账本可比可加.

| 对比       | 主流做法                              | 本设计                    | pro                              | con                 | 假设                      |
| ---------- | ------------------------------------- | ------------------------- | -------------------------------- | ------------------- | ------------------------- |
| 多周期聚合 | (a) 统一模型多任务 (b) pod 各速度独立 | (b): 账本制, label=各自 W | 模块化/可独立验收/加账本零重构   | 放弃跨周期联合信息  | 跨周期交互的钱 < 复杂度税 |
| 慢层模型   | Barra 式线性 / 部分店上 ML            | 线性 + 双收缩 (IC 与 R_z) | 估计方差≈0, 可归因               | 零非线性/零状态依赖 | 慢层独立样本 ~10² (P1)    |
| 快层模型   | GBDT 主流, NN 少数                    | GBDT + 严格 CV 卫生       | 样本预算内最优性价比, 交互自动学 | 归因弱于线性        | 有效样本≥10⁶              |
| 端到端     | raw data → NN 直出仓位 (少数前沿)     | 分层: 因子→预测→组合      | 每层可测可换可归因               | 理论上限低于端到端  | 个人/小团队维护带宽       |

---

## 5. 组合层 (账本内)

每账本独立把 μ 变成**目标权重向量** w_b (出口统一为权重, 槽/等权等离散概念只允许存在于账本内部).

**连续形态** (默认):

```text
w_b,i = clip( (μ_b,i − μ_ref) / (λ·σ_i²), 0, w_max ) , 归一化 Σ_i w_b,i = 1   # 满仓
μ_ref = 池内 μ 的第 q 分位 (纯多头的机会成本锚: 不持有 ≠ 收益 0, 而是持有池内边际标的)
σ_i² = σ_idio,i² + β_i,ind²·σ_ind²     # 对角 + 行业块; 不上全协方差 (N 小估不动, P1)
```

**离散形态** (集中持仓 N≈10 的慢账本特例): top-N 等权 + **交换规则**:

```text
换 (卖 A 买 B, 数量 q) 当且仅当:
  (1) Δμ = μ_B − μ_A > cost_rt(q)                    # 成本门槛 (P2)
  (2) Δμ / se(Δμ) > κ                                # 信心门槛, κ ≈ 1 (P2; se 由预测层误差棒传播)
数量:  q* = argmax_q [ q·Δμ − cost_rt(q) ]           # 凸成本 ⇒ 内点解, 部分换仓是普通输出
```

性质 (皆为推导结果, 非设计选择): 滞回带内生 (成本项即带宽, 且随截面离散度自适应); 高方差/信号打架时自动粘滞在现状 (se 大 ⇒ 门槛难过); 涨后掉排名的持仓只有被"超过成本"的幅度击败才出局 (机会成本以 μ_A 现值入式, 防提前止盈).

**明确不做**: Kelly 式连续加杠杆 (满仓约束下无意义)、全协方差 MVO (N≈10 下估计误差 > 优化增益)、对正偏跳跃的偏度修正 (偏度不可估, P3 — 接受 MV 对彩票仓位的过度保守).

| 对比     | 主流做法                | 本设计                      | pro                      | con                      | 假设                        |
| -------- | ----------------------- | --------------------------- | ------------------------ | ------------------------ | --------------------------- |
| 组合构造 | QP 优化器 (含成本罚项)  | 连续权重闭式 / 离散交换规则 | 零求解器依赖, 行为可解释 | 放弃约束间联合最优       | 集中组合下排序噪声≫权重增益 |
| 换仓触发 | 日历再平衡 / 固定缓冲带 | 成本+信心双门槛, 数量内点   | 换手逐笔自证盈利 (P2)    | 需要可信的 se 与成本模型 | 成本模型可校准              |

---

## 6. 聚合层

```text
W_i(t) = Σ_b c_b · w_b,i(t)      # 真实账户目标权重 = 账本权重的资金配比加权和, Σ_b c_b = 1
```

- **账本互不可见**. 净额化自动发生: 慢要卖 X、日内要买 X ⇒ 目标层抵消, 不出市场、不付成本.
- **资金配比 c_b** = 跨账本唯一自由参数. 定法: 风险预算 — `c_b ∝ max(0, SR_b^net) / σ_b` 按账本相关阵修正 (或等风险贡献 ERC); SR_b^net 来自 L9 的 shadow book 费后夏普. 季度更新, 单次变动上限 ±20% (防配比自身成为过拟合面).
- 新账本上线: c_b 从小额 (≤5%) 起步, 由 ΔPnL 证据逐季爬坡.

| 对比     | 主流做法                         | 本设计            | pro                                 | con                  | 假设            |
| -------- | -------------------------------- | ----------------- | ----------------------------------- | -------------------- | --------------- |
| 跨速聚合 | 中央风险账本 (多经理平台) / 单簿 | 账本加权和 + 净额 | 结构同平台级实践, 缩到 1 人规模可行 | 无跨账本联合风险优化 | 账本数 ≤ 个位数 |

---

## 7. 执行层 (中央, 唯一碰真实账户的层)

**核心转念**: 不存在"当日交易清单"; 存在的是一个*移动的聚合目标* W(t) 和一个*被约束的追踪问题*. 未完成偏差不是故障, 是跨日滚动的跟踪误差 (T+1/涨跌停/流动性截断都只是"今天追不完").

**状态定义**:

```text
pos_i(t)       # 真实持仓 (股)
sellable_i(t)  = pos_i(昨日收盘) − 今日已卖_i(t)          # T+1 可卖库存, 共享资源
dev_i(t)       = W_i(t)·PV − pos_i(t)·P_i(t)             # 目标偏差 (元)
```

**追踪优化** (每 epoch 解一次, 如 5 分钟; per 票近独立 + 现金耦合, 规模极小):

```text
min_x   Σ_i u_i·( dev_i − x_i )² + Σ_i cost_i(x_i)
s.t.    x_i ≥ −sellable_i·P_i          # T+1
        |x_i| ≤ π·ADV_i^intraday       # 参与率上限 π
        涨停: x_i ≤ 0 之物理禁买; 跌停/停牌: x_i = 0 (物理) — 规则层可再收紧
        Σ_i x_i ≤ cash; 手数取整在净单层, 尾差入台账
```

- **紧迫度 u_i**: 两档足够 — 偏差主要贡献账本是快账本 ⇒ 当日追 (它的 alpha 过期即目标自动移回, 未成交意图自然作废); 慢账本 ⇒ 多日 TWAP/被动限价分批. 不需要连续 urgency 曲线.
- **可卖库存分配**: 多账本争卖同一票时按净单贡献比例分配 sellable (默认 pro-rata; 快账本可加权).
- **成交摊回 + 成本内化**: 成交与其成本按贡献比例摊回各账本虚拟台账. 真实持仓 = Σ 虚拟台账 (近似, 取整尾差由台账吸收). 每账本的考核 PnL 含自己引发的成本 — 防快账本白嫖换手.
- **冲击成本模型** (平方根律):
  ```text
  cost_rt(q) = fees + spread/2 + η·σ_daily·√( q / ADV )
  η 初值 0.5 (薄流动性小票偏保守), 上线后用实盘成交价 vs 决策价残差回归持续校准
  ```
  这是全系统最值得花精力校准的模型 — 交换规则、追踪优化、账本考核全部消费它.
- **执行算法选择**: 慢偏差 → 被动限价/TWAP 多日; 快偏差 → 限价追单当日; 规则层强平 → 即时市价系. 委托拆分服从参与率上限.

| 对比     | 主流做法                  | 本设计                        | pro                        | con                | 假设               |
| -------- | ------------------------- | ----------------------------- | -------------------------- | ------------------ | ------------------ |
| 执行组织 | OMS/EMS 追目标 + 算法执行 | 同构: 追踪优化器 + 简化算法层 | "截断"常态化, T+1 自然表达 | 需自建成交回填闭环 | 委托可程序化 (QMT) |
| 成本模型 | 券商 TCA / 自研冲击模型   | 平方根律 + 实盘残差自校准     | 一个参数, 可持续收敛       | 冷启动期误差大     | 成交样本可累积     |

---

## 8. 规则层 (事件 override, 最高优先级)

**定义**: 规则 = *可观测状态*触发的强制执行指令. 参数由业务先验定死, **不搜索不优化** (P3); 数据只通过事件研究行使否决权. 规则不进任何模型、不进因子权重 — 把事件编码成因子是范畴错误 (事件是条件策略, 不是截面信号).

| 规则          | 触发 (可观测)      | 动作               | 先验依据                 |
| ------------- | ------------------ | ------------------ | ------------------------ |
| 涨停惯性      | 收盘封死涨停       | 禁卖 (持有过夜)    | 封板隔夜溢价             |
| 炸板止盈      | 盘中触板、收盘未封 | 强制卖出 (老仓)    | 跳跃终结信号, 右尾已实现 |
| 跌停          | 收盘跌停           | 禁买 (物理禁卖)    | T+1 下接跌停刀无保护     |
| 退市/风险警示 | 标识生效           | 强制清仓           | 尾部不可估 (P3)          |
| 停牌          | 停牌中             | 冻结, 目标偏差挂起 | 物理约束                 |

**验收协议** (每条规则独立): 事件对齐累计超额 `CAR(τ) = mean[事件后 τ 日累计超额]`, 规则 on/off 两版对比, 时间块 bootstrap 置信区间; 加上 L9 全局 ΔPnL. 数据否决 ⇒ 规则下线, 不改参数重试 (改参数重试 = 变相拟合).

| 对比     | 主流做法            | 本设计              | pro                      | con                    | 假设                          |
| -------- | ------------------- | ------------------- | ------------------------ | ---------------------- | ----------------------------- |
| 事件处理 | 当特征喂模型 / 忽略 | 独立规则层 + 只验收 | 零自由度消耗, 样本少也稳 | 放弃事件强度的连续刻画 | 事件 alpha 主体在方向不在强度 |

---

## 9. 验收与归因层 (旁路, 管一切准入与存续)

**唯一准入标准 — ΔPnL**: 任何新组件 X (因子/账本/规则/参数变更):

```text
接纳 X ⟺ walk-forward 中 净PnL(系统+X) − 净PnL(系统) 的夏普差 > 0, 且时间块 bootstrap p < 0.1
```

IC、单测夏普、胜率一律只是诊断量, 不是准入依据 — 高 IC 低 ΔPnL 的信号 (把趋势切碎型) 必须被这道门挡住.

**shadow book 阶梯**: `慢 → 慢+中 → 慢+中+隔夜 → 全账本 → 全账本+规则` 五档影子净值常开, 每档边际夏普即各层的持续存在理由. 某档边际转负且持续 ⇒ 降配比直至下线.

**因子存续**: 滚动重估 IC* — 连续 2 个季度 `|IC*|/se < 1` 的因子在下次重估中权重自然归零 (经收缩机制, 无手动 kill 的戏剧性). 档案卡年检: 因子截面分布年度 KDE 漂移、ρ(h) 漂移.

**实盘对账**: 决策代码回测/实盘同源单份; 实盘成交价回填冲击模型; 每日实盘持仓 vs 目标偏差分解为 {T+1 截断, 涨跌停, 流动性, 取整} 四项, 异常项报警.

| 对比     | 主流做法         | 本设计                      | pro                                | con        | 假设       |
| -------- | ---------------- | --------------------------- | ---------------------------------- | ---------- | ---------- |
| 准入标准 | IC/ICIR 门槛为主 | ΔPnL 唯一, IC 仅诊断        | 直接对齐最终目标, 防切碎趋势型信号 | 验收算力大 | 回测引擎快 |
| 归因     | 事后月度归因     | shadow book 常开 + 成本内化 | 层级边际贡献实时可见               | 台账工程量 | —          |

---

## 10. 自由参数总账 (过拟合预算)

| 参数           | 层  | 定法                         | 个数     |
| -------------- | --- | ---------------------------- | -------- |
| γ (岭)         | L4  | 慢账本 CV 粗扫, 定后不动     | 1        |
| λ (风险厌恶)   | L5  | 目标波动反推                 | 1        |
| κ (信心门槛)   | L5  | ≈1, 先验                     | 1        |
| η (冲击系数)   | L7  | 实盘残差回归 (自校准)        | 1        |
| π (参与率上限) | L7  | 先验 (如 5% intraday ADV)    | 1        |
| c_b (资金配比) | L6  | 风险预算公式, 季调带变动上限 | 账本数−1 |
| IC 估计窗口    | L3  | 全库统一 (如滚动 3 年)       | 1        |
| GBDT 超参      | L4  | 全局一套, 定后不动           | ~5       |

**per-factor 自由参数 = 0; per-rule 参数 = 先验 (只可否决不可调优)**. 全系统可调总量 ≈ 15 个, 其中一半有自校准或公式定法. 这张表本身是防过拟合的核心装置: 任何新设计若要求新增参数, 先在此登记并说明定法.

---

## 11. 落地顺序 (每阶段独立可验收, 前序不返工)

1. **档案卡基建**: IC(h) 面板 (Spearman, label 不截尾) + ρ(h) + 事件曲线, 全量存量因子过一遍. 纯测量, 零行为变更.
2. **慢账本口径对齐**: 组合权重的评价 label 换到 W (含成本罚项); Σ⁻¹·IC* 闭式解与格点搜索互为对照. 验收: 两法权重方向一致 + walk-forward 费后不劣于现状.
3. **交换规则替换固定带宽**: 成本+信心双门槛 + 内点数量; 现有带宽版留作 baseline. 验收: ΔPnL ≥ 0.
4. **执行层跟踪化**: 目标追踪 + sellable 台账 + 冲击模型冷启动 (η=0.5), 实盘成交回填开始积累.
5. **隔夜账本** (分钟数据就绪后): 首个 GBDT 账本, c_b ≤ 5% 起步, 走完整 L9 准入.
6. **日内账本** (最后): 依赖 4 的执行成熟度与 5 的 CV 卫生经验; 仅操作老库存 (T+1 合法域).

阶段间的普适门槛: 上一阶段的 shadow book 边际夏普为正、且实盘 vs 回测偏差分解无未解释项.
